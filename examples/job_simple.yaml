# Minimal job.yaml — single grid sweep, no overrides.
# Preview: runq submit job_simple.yaml --dry   → lr(3) × batch_size(2) = 6 tasks
project: resnet50
sweep:
  - method: grid
    parameters:
      lr: [0.001, 0.01, 0.1]
      batch_size: [32, 64]
